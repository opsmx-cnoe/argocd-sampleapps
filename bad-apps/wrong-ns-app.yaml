apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    name: namespace-configmap-app
  name: namespace-configmap-app
  namespace: argocd
spec:
  destination:
    namespace: wrong-default
    server: https://dev-vcluster.opencd.opsmx.org:443
  project: default
  source:
    directory:
      recurse: false
    path: bad-apps/bad-manifest/bad-configmap
    repoURL: https://github.com/opsmx-cnoe/naveen
    targetRevision: main
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
