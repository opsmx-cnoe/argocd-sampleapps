apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    name: path-configmap-app
  name: path-configmap-app
  namespace: argocd
spec:
  destination:
    namespace: default
    server: https://dev-vcluster.opencd.opsmx.org:443
  project: default
  source:
    directory:
      recurse: false
    path: wrong-bad-apps/bad-config
    repoURL: https://github.com/opsmx-cnoe/naveen
    targetRevision: main
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true

