 apiVersion: v1
kind: Service
metadata:
  name: bad-service
  namespace: default
spec:
  selector:
    app: bad-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080  # ❌ Invalid: should be an integer


