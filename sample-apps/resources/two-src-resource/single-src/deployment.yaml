apiVersion: apps/v1
kind: Deployment
metadata:
  name: two-src-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: two-src-deployment
  template:
    metadata:
      labels:
        app: two-src-deployment
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: two-src-deployment
